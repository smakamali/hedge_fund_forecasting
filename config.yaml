# Input lag configuration for run_validation_lagged_features.py
# Used when running the script directly (not by experiment scripts).
input_lags:
  lags_max: 5
  top_k_per_feature: 2
  use_global_lags: false
  global_lags: null   # or [1, 2, 3, 5] when use_global_lags is true
